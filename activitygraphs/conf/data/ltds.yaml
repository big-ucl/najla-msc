# @package data
name: LTDS0515

files:
  raw_household: Household.parquet
  raw_person: Person.parquet
  raw_trip: Trip.parquet
  raw_stage: Stage.parquet

paths:
  processed: data/processed/${..name}
  raw: data/raw/${..name}
  act_dataset: ${..paths.processed}/act_dataset
  graphs: ${..paths.processed}/graphs
  metrics: ${..paths.processed}/metrics
  