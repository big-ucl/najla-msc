defaults:
  - _self_
  - data: toronto

paths:
  reports: reports
  figures: ${..paths.reports}/figures
  models: models

train:
  epochs: 50

  fast_dev_run: false
  overfit_batches: 0
  schedule_lr: true
  debug: false

  wandb: true
  wandb_project: najla-msc
  wandb_entity: najla-hs-alqahtani-ucl